using System.Collections.Generic;
using System.Linq;
using GetTheMilk.Actions.Interactions;

namespace GetTheMilk.Utils
{
    public static class CoreExtensions
    {
        //public static IEnumerable<Interaction> GetAllAplicableInteractionRules(this SortedList<string,Interaction[]> allRules,string forCharacter )
        //{
        //    return ((!allRules.ContainsKey(GenericInteractionRulesKeys.All))?(new Interaction[0]):allRules[GenericInteractionRulesKeys.All])
        //        .Union((!allRules.ContainsKey(GenericInteractionRulesKeys.CharacterSpecific))
        //            ?(new Interaction[0]) : allRules[GenericInteractionRulesKeys.CharacterSpecific]).
        //            Union((!allRules.ContainsKey(forCharacter))?(new Interaction[0]):allRules[forCharacter]);
        //}

    }
}
